import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:fiscal/app/modules/peso/peso_module.dart';

void main() {
  initModule(PesoModule());
  // PesoController peso;
  //
  setUp(() {
    //     peso = PesoModule.to.get<PesoController>();
  });

  group('PesoController Test', () {
    //   test("First Test", () {
    //     expect(peso, isInstanceOf<PesoController>());
    //   });

    //   test("Set Value", () {
    //     expect(peso.value, equals(0));
    //     peso.increment();
    //     expect(peso.value, equals(1));
    //   });
  });
}
