import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('FiscalizacaoPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(FiscalizacaoPage(title: 'Fiscalizacao')));
    //  final titleFinder = find.text('Fiscalizacao');
    //  expect(titleFinder, findsOneWidget);
  });
}
