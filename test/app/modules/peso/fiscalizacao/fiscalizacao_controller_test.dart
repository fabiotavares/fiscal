import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:fiscal/app/modules/peso/peso_module.dart';

void main() {
  initModule(PesoModule());
  // FiscalizacaoController fiscalizacao;
  //
  setUp(() {
    //     fiscalizacao = PesoModule.to.get<FiscalizacaoController>();
  });

  group('FiscalizacaoController Test', () {
    //   test("First Test", () {
    //     expect(fiscalizacao, isInstanceOf<FiscalizacaoController>());
    //   });

    //   test("Set Value", () {
    //     expect(fiscalizacao.value, equals(0));
    //     fiscalizacao.increment();
    //     expect(fiscalizacao.value, equals(1));
    //   });
  });
}
