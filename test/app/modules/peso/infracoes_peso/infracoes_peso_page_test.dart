import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('InfracoesPesoPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(InfracoesPesoPage(title: 'InfracoesPeso')));
    //  final titleFinder = find.text('InfracoesPeso');
    //  expect(titleFinder, findsOneWidget);
  });
}
