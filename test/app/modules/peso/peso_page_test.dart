import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('PesoPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PesoPage(title: 'Peso')));
    //  final titleFinder = find.text('Peso');
    //  expect(titleFinder, findsOneWidget);
  });
}
