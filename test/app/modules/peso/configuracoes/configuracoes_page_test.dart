import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('ConfiguracoesPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ConfiguracoesPage(title: 'Configuracoes')));
    //  final titleFinder = find.text('Configuracoes');
    //  expect(titleFinder, findsOneWidget);
  });
}
