void main() {
//  testWidgets('TextFieldSomaWidget has message', (tester) async {
//    await tester.pumpWidget(buildTestableWidget(TextFieldSomaWidget()));
//    final textFinder = find.text('TextFieldSoma');
//    expect(textFinder, findsOneWidget);
//  });
}
